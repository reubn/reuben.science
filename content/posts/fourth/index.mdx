export const metadata = {
  title: 'Cooking the Perfect Pizza',
  description: 'Sourdough pizza from scratch',

  emoji: '🍕',
  date: '2020-12-07',
  category: ['food']
};

``` jsx JSX
// return incremented value this isnt a workable solution really is it?
var foo = function (bar) {
  return bar++;
};

const test = {
  a: 6,
  b: 12
}

console.log(foo(5));
getComputedStyle(document.documentElement).getPropertyValue('--color-primary');

js^if (process.browser) {
  const Mdx = dynamic(() => import(`../../content/posts/${slug}`));

  mdx = <Mdx />;
} else {
  const Component = require(`../../content/posts/${slug} ${text + 6 % 100}`).default;
  const ReactDOMServer = require('react-dom/server');

  const ssr = ReactDOMServer.renderToString(<Component />);

  mdx = <div dangerouslySetInnerHTML={{ __html: ssr }} />;
}
```
caption: My code *caption*

Here is a footnote reference,[^b]
another,[^longnote],
and optionally there are inline
notes.^[you can type them inline, which may be easier, since you don’t
have to pick an identifier and move down to type the note.]

[^b]: Here is the footnote.

[^longnote]: Here’s one with multiple blocks.

    Subsequent paragraphs are indented to show that they
belong to the previous footnote.

        { some.code }

    The whole paragraph can be indented, or just the first
    line.  In this way, multi-paragraph footnotes work like
    multi-paragraph list items.

This paragraph won’t be part of the note, because it
isn’t indented.

![Stormtroopocat](https://images.unsplash.com/photo-1564415900645-30612d54dd0c?ixlib=rb-1.2.1&auto=format&fit=crop&w=2852&q=80 'The Stormtroopocat')
caption: Oranges en La Mezquita de Sevilla
