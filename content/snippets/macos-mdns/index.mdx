export const metadata = {
  title: 'Copy Files over SSH',
  description: 'Using rsync',
  emoji: '📂',
  date: '2021-07-28',
  preview: {
    language: 'bash',
    content: "rsync -avzr -e 'ssh' $LOCAL_PATH $SSH_CONNECTION_STRING:$REMOTE_PATH"
  }
}

```bash
rsync -avzr -e 'ssh' $LOCAL_PATH $SSH_CONNECTION_STRING:$REMOTE_PATH
```
