export const metadata = {
  title: 'Fix Damaged macOS Applications',
  description: 'Fix Damaged macOS Applications',
  emoji: '💀',
  date: '2021-07-28'
}

```bash
xattr -c /Path/To/Application
```

```bash
sudo codesign --force --deep --sign - /Path/To/Application
```
