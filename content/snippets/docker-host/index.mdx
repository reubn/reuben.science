export const metadata = {
  title: 'Docker Build with Host Networking',
  description: 'Build Docker Containers with Host Networking',
  emoji: '🐳',
  date: '2021-07-28'
}

```bash
docker build --network=host -t $IMAGE ./
```
