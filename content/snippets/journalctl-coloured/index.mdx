export const metadata = {
  title: 'Coloured Reversed Output for Systemd Services',
  description: 'Preserve Colours in journalctl',
  emoji: '🌈',
  date: '2021-07-28',
  preview: {
    language: 'bash',
    content: 'sudo journalctl -u $unit -r -o cat'
  }
}

```bash
sudo journalctl -u $unit -r -o cat
```
